# Core — required for load_dataset() in dataset.py / generate_metadata.py
pandas>=1.5

# Optional — only needed if you use GuitarTuningDataset from dataset.py
soundfile>=0.12
torch>=2.0

# Notebook — required to run acoustic-guitar-data-v.ipynb (EDA)
numpy>=1.24
librosa>=0.10
matplotlib>=3.7
scikit-learn>=1.2
